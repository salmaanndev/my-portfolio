# Salman Ahmad — Portfolio

Animated personal site built with React, TypeScript, Vite, Tailwind CSS, and Framer Motion. Content is sourced from the latest CV.

## Run locally

```bash
npm install
npm run dev
```

Open the printed local URL (usually `http://localhost:5173`).

```bash
npm run build
npm run preview
```
